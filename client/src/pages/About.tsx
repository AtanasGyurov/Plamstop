export default function About() {
  return (
    <section className="card">
      <h2 style={{marginTop:0}}>About</h2>
      <p style={{color:"var(--muted)"}}>
        Minimal MERN + TypeScript starter for your shop.
      </p>
    </section>
  );
}
